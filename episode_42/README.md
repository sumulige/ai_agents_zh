# Episode 42: 100% FREE Explainer Videos with n8n and Z-Image

> AI Agents A-Z - 用 n8n 构建实用的 AI Agent
> 难度: ⭐⭐ | 预估时间: 30 分钟

---

## [Level 1] 这一集在做什么？

本集教你用 n8n 自动化创建**完全免费的说明性视频**（explainer videos）。

**一句话**：像"视频工厂"一样，输入任何想法，自动生成包含配音和画面的完整视频。

**适用场景**：
- YouTube 频道的批量视频制作
- 产品说明视频自动化
- 教育内容快速生成

> 💡 **快速判断**：如果你需要**零成本**批量制作视频内容，这一集适合你。
> 想了解更多？继续阅读 [Level 2]。

---

## [Level 2] 核心概念

### 你会学到什么

| 序号 | 概念 | 说明 |
|------|------|------|
| 1 | **Modal 免费 GPU** | 使用 Modal.com 的免费额度运行 AI 模型 |
| 2 | **Z-Image Turbo** | 超快速 AI 图像生成模型 |
| 3 | **Kokoro TTS** | 高质量文字转语音引擎 |
| 4 | **n8n 工作流** | 连接表单、LLM 和 Modal 服务器 |

### 涉及的 n8n 节点

| 节点类型 | 用途 | 新手友好度 |
|----------|------|------------|
| Form Trigger | 收集用户输入 | ⭐ 简单 |
| LLM Chain (OpenAI) | 生成视频场景脚本 | ⭐⭐ 中等 |
| HTTP Request | 调用 Modal API | ⭐⭐ 中等 |
| Code | 数据格式转换 | ⭐⭐ 中等 |

### 涉及的外部服务

| 服务 | 免费额度 | 难度 | 官网 |
|------|----------|------|------|
| **Modal** | $30 免费额度/月 | ⭐⭐ | [modal.com](https://modal.com/) |
| **Z-Image Turbo** | 通过 Modal 免费使用 | ⭐ | [HuggingFace](https://huggingface.co/Tongyi-MAI/Z-Image-Turbo) |
| **OpenAI API** | 按使用付费 | ⭐ | [openai.com](https://openai.com/) |

> 💡 **了解够了？** 知道学什么就可以开始。继续阅读 [Level 3] 了解工作流结构。

---

## [Level 3] 工作流结构

### 工作流概览图

```
┌─────────────────────────────────────────────────────────────┐
│              "Explainer Video Creator" 工作流                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  [Form Trigger] ──► [Setup Defaults] ──► [HTTP Request]    │
│  用户输入表单      服务器URL检查        测试连接             │
│       │                 │                                   │
│       │                 └───► [Configuration]              │
│       │                       艺术风格配置                  │
│       │                       │                            │
│       └───────────────────────┼──────────────┐            │
│                               │              │            │
│                               ▼              ▼            │
│                        [LLM Chain]    [Server Error]      │
│                        创建场景脚本      连接失败处理        │
│                               │                            │
│                               ▼                            │
│                        [Add Art Style]                     │
│                        格式化图像提示                        │
│                               │                            │
│                               ▼                            │
│                        [HTTP Request]                      │
│                        调用 Modal API                      │
│                               │                            │
│                               ▼                            │
│                         [返回视频文件]                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Modal 服务器架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Modal 服务器架构                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  n8n ──► Flask API ──► ExplainerVideoGenerator             │
│                              │                              │
│                              ├──► ZImageTurboModel         │
│                              │     (生成图像)               │
│                              │                              │
│                              └──► KokoroTTSModel          │
│                                    (生成语音)               │
│                                                             │
│  输出: MP4 视频文件                                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 数据流

```
用户输入内容
    │
    ├──► LLM 解析为场景列表
    │     └──► scenes: [{ narration, image_prompt }, ...]
    │
    ├──► 格式化提示词
    │     └───► 添加艺术风格配置
    │
    └──► 发送到 Modal 服务器
          └──► 生成视频 (图像 + TTS + 合成)
```

### 节点说明

| 节点 | 类型 | 配置要点 | 数据输出 |
|------|------|----------|----------|
| **On form submission** | Form Trigger | 收集：内容、Modal URL、额外上下文 | 表单数据 |
| **Setup defaults** | Set | 清理 URL 格式 | `MODAL_SERVER_URL` |
| **HTTP Request** | HTTP Request | 测试服务器连接 | 200 OK / Error |
| **Configuration** | Set | 定义艺术风格配置 | `art_style` 对象 |
| **OpenAI Chat Model1** | LLM | 使用 JSON 模式输出 | 结构化场景 |
| **Structured Output Parser** | Output Parser | 定义 scenes JSON Schema | `{scenes: [...]}` |
| **Create scenes** | LLM Chain | 生成视频场景和提示词 | `{output: {scenes}}` |
| **Add art style** | Code | 格式化 image_prompt | `{scenes: [...]}` |
| **HTTP Request1** | HTTP Request | POST 到 `/generate-video` | 视频文件 |

> 💡 **准备就绪？** 理解工作流结构后，继续阅读 [Level 4] 开始构建。

---

## [Level 4] 构建步骤

### 前置准备

在开始之前，请确保：

- [ ] n8n 已安装并运行（访问 http://localhost:5678）
- [ ] Modal 账号已创建 ([modal.com](https://modal.com/)）
- [ ] Modal CLI 已安装 (`pip install modal`)
- [ ] OpenAI API Key 已配置
- [ ] Python 3.11+ 已安装（用于 Modal 服务器）

### 步骤 1: 部署 Modal 服务器

**目标**: 在 Modal 上部署视频生成服务

**操作**:

1. 下载 `modal_explainer_videos.py` 文件

2. 安装 Modal CLI:
```bash
pip install modal
```

3. 登录 Modal:
```bash
modal token new
```

4. 部署服务器:
```bash
modal deploy modal_explainer_videos.py
```

5. 获取服务器 URL（部署后会显示）

**验证**: 访问服务器 URL，应该看到 `{"status": "alive"}`

**预期结果**: Modal 服务器运行中，可以接收视频生成请求

---

### 步骤 2: 导入 n8n 工作流

**目标**: 将工作流导入 n8n

**操作**:

1. 在 n8n 中点击右上角 **"..."** 菜单
2. 选择 **"Import from File"**
3. 选择 `n8n_workflow_explainer_video_creator.json`

**验证**: 工作流应该显示在画布上

---

### 步骤 3: 配置 LLM 模型

**目标**: 选择并连接你喜欢的 LLM

**操作**:

1. 找到 "Pick your choice of LLM" 区域
2. 选择一个 LLM 节点（如 OpenAI Chat Model1）
3. 配置 API 凭证
4. **重要**: 删除或禁用其他未使用的 LLM 节点

**可用选项**:
- OpenAI (GPT-4.1-mini) - 推荐
- Google Gemini
- Anthropic Claude
- DeepSeek
- Groq
- Ollama (本地)

**验证**: 点击 LLM 节点，凭证已连接

---

### 步骤 4: 配置表单触发器

**目标**: 设置用户输入表单

**操作**:

1. 点击 **"On form submission"** 节点
2. 在 "Production URL" 选项中点击 **"Copy URL"**
3. 访问 URL 测试表单

**表单字段**:
- **Content** (必需): 视频内容主题
- **Your modal server URL** (必需): Modal 服务器地址
- **Extra context** (可选): 额外上下文信息

---

### 步骤 5: 测试完整工作流

**目标**: 端到端验证

**操作**:

1. 激活工作流（点击左上角切换开关）
2. 填写表单：
   - Content: "Why coffee makes you productive"
   - Modal Server URL: `https://your-server.modal.run`
3. 提交表单
4. 等待处理（约 1-3 分钟）

**预期结果**: 工作流执行完成后返回视频文件

> 💡 **需要帮助？** 如果遇到问题，查看 [Level 5] 故障排除。

---

## [Level 5] 进阶内容

### 自定义艺术风格

**Configuration 节点** 默认使用 "2D hand-drawn animation" 风格。修改 `art_style` 对象可以自定义：

```json
{
  "art_style": "3D Pixar style",
  "visual_style": "vibrant colors, detailed textures, cinematic lighting",
  "character_design": "friendly 3D characters with expressive faces",
  "background_style": "detailed 3D environments",
  "color_palette": "bright, saturated colors",
  "mood": "upbeat, energetic, family-friendly"
}
```

### 调整视频参数

在 HTTP Request1 节点中，可以修改：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `voice` | `af_heart` | TTS 声音 (更多: `af_heart`, `am_michael`, `bf_emma`) |
| `tts_speed` | 1.0 | 语音速度 (0.5-2.0) |
| `image_width` | 1920 | 视频宽度 |
| `image_height` | 1080 | 视频高度 |

### 可用的 Kokoro 声音

| 语音代码 | 性别 | 风格 |
|----------|------|------|
| `af_heart` | 女 | 温暖友好 |
| `af_bella` | 女 | 清晰专业 |
| `am_michael` | 男 | 平稳叙述 |
| `bf_emma` | 女 | 活泼热情 |
| `bm_george` | 男 | 深沉权威 |

### 故障排除

| 问题 | 症状 | 可能原因 | 解决方案 |
|------|------|----------|----------|
| 服务器连接失败 | "Server isn't ready" 错误 | Modal 服务器未部署或 URL 错误 | 检查服务器 URL 格式，确保包含 `https://` |
| LLM 输出格式错误 | 工作流在 Create scenes 失败 | LLM 未启用 JSON 模式 | 确保使用支持 JSON 模式的模型 |
| 视频生成超时 | 请求超时 | Modal 容器超时 | 增加 Modal 函数的 timeout 参数 |
| 图像质量差 | 生成的图像不理想 | 提示词不够详细 | 修改 LLM prompt，增加艺术风格描述 |

### 生产部署注意事项

**环境变量管理**:
```python
# 在 Modal 服务器中使用 secrets
modal.Secret.from_name("openai_api_key")
```

**成本优化**:
- Modal 新用户有 $30 免费额度
- Z-Image Turbo 使用量化模型，GPU 成本低
- 监控 Modal 使用量避免超支

**性能优化**:
- 增加 Modal GPU 并发数（修改 `gpu="L40s"` 数量）
- 使用 Modal Volume 缓存模型
- 调整 SDNQ 量化参数平衡质量和速度

### 相关资源

**相关 Episode**:
- [Episode 40](../episode_40/) - Flux.2[dev] 视频生成
- [Episode 22](../episode_22/) - 长视频生成工作流
- [Episode 13](../episode_13/) - MiniMax Hailuo 2 视频生成

**外部资源**:
- [Modal 文档](https://modal.com/docs)
- [Z-Image Turbo 论文](https://arxiv.org/abs/2407.21729)
- [Kokoro TTS GitHub](https://github.com/remsky/Kokoro-Fast-API)

---

## 资源下载

### n8n 工作流文件

下载并导入到 n8n：

- [n8n_workflow_explainer_video_creator.json](./n8n_workflow_explainer_video_creator.json)

**导入方法**:
1. 在 n8n 中点击右上角 "..." 菜单
2. 选择 "Import from File"
3. 选择下载的 JSON 文件

### Modal 服务器代码

- [modal_explainer_videos.py](./modal_explainer_videos.py)

**部署方法**:
```bash
modal deploy modal_explainer_videos.py
```

---

## 观看视频

[![100% FREE explainer videos with n8n and Z-Image
](https://img.youtube.com/vi/zesrIoxxymo/0.jpg)](https://www.youtube.com/watch?v=zesrIoxxymo)

**时长**: ~15 分钟 | **更新日期**: 2025-01-16

---

## 社区支持

遇到问题？加入社区获取帮助：

- [Skool 社区](https://www.skool.com/ai-agents-az/about?w9)
- 获取 Premium 版本工作流（包含声音克隆、片头片尾生成等）

---

## 导航

| 你的需求 | 建议阅读 |
|----------|----------|
| 快速了解本集内容 | Level 1 |
| 决定是否学习本集 | Level 1-2 |
| 理解工作流原理 | Level 3 |
| 跟随教程构建 | Level 4 |
| 排查问题/生产部署 | Level 5 |

---

**Episode**: 42 | **版本**: v2.0 (分层解释版) | **最后更新**: 2025-01-16

**标签**: n8n, video generation, Z-Image, Kokoro TTS, Modal, free, explainer video
